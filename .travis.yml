sudo: false

branches:
  only:
    - documenting-fbi 

addons:
  apt:
    packages:
      - doxygen

script:
  - doxygen DOXYFILE 

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/html
  github_token: $GH_REPO_TOKEN
  on:
    branch: master
